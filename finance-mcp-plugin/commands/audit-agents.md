---
description: Inventory every registered agent on a PE MCP server, flag zombies (idle too long), runaway-cost agents (modeled monthly spend over a threshold), and misaligned agents (eval rubric fail), and emit a board-defendable pruning recommendation with annual savings if pruned. Pure deterministic — Anthropic public list prices × per-family token-budget envelope, no LLM call. Telemetry is modeled and labelled as such.
---

Load the audit-agents skill from skills/private-equity/audit-agents/SKILL.md and run the auditor following the pipeline in that skill. Pass through any user arguments: $ARGUMENTS
